{
  "name": "keystone-suite",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cleanup": "rimraf packages/*/lib",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build": "pnpm run cleanup && pnpm -r --filter \"./packages/**\" build",
    "dev": "pnpm run cleanup && pnpm -r --filter \"./example\" run start & pnpm -r --filter \"./packages/**\" run watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "pnpm run cleanup && pnpm -r --filter \"./packages/**\" publish --no-git-checks --access public --report-summary"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.49.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
